<?xml version="1.0" encoding="utf-8"?>
<!-- Kroste-Standard: So viel wie moeglich loggen. Datei = Trace aufwaerts,
     Konsole = Info aufwaerts. Secrets werden ueber den masked-Layout-Renderer
     maskiert (MaskingLayoutRenderer wird im Programmstart registriert). -->
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Warn"
      internalLogFile="logs/nlog-internal.log">

  <targets>
    <target xsi:type="File" name="file"
            fileName="logs/nougat.log"
            archiveFileName="logs/archive/nougat.{#}.log"
            archiveEvery="Day"
            maxArchiveFiles="14"
            layout="${longdate}|${level:uppercase=true}|${logger}|${masked:inner=${message}}${onexception:inner=|${exception:format=tostring}}" />

    <target xsi:type="ColoredConsole" name="console"
            layout="${time}|${level:uppercase=true}|${logger:shortName=true}|${masked:inner=${message}}${onexception:inner=|${exception:format=message}}" />
  </targets>

  <rules>
    <!-- HttpClient-Rauschen filtern: Microsoft.Extensions.Http maskiert Header
         standardmaessig als '*' und schreibt fuer jeden Request 4 Bloecke mit
         allen Response-Headern - erzeugt megabyteweise nutzloses Log. -->
    <logger name="System.Net.Http.*" maxlevel="Debug" final="true" />
    <logger name="System.Net.Http.*" minlevel="Warn" writeTo="file,console" />

    <logger name="*" minlevel="Trace" writeTo="file" />
    <logger name="*" minlevel="Info" writeTo="console" />
  </rules>
</nlog>
